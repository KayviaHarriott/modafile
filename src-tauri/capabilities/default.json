{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the KiloFile desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
